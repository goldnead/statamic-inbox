# npm 10 cannot resolve vitest's optional peer set next to a file: dependency
# (arborist dies on "reading 'edgesOut'"). The lock was built this way, and
# `npm ci` in the assets job needs the same setting to accept it.
legacy-peer-deps=true
